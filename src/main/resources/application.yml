server:
  port: 80


spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/reptile?useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.jdbc.Driver
#    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
logging:
  level:
    root: INFO
